# FPP WLED API Proxy - Root Level Rewrite Rules
# Routes WLED API requests to the plugin handler

<IfModule mod_rewrite.c>
    RewriteEngine On

    # Test without any conditions - just check if ANY request triggers this
    RewriteRule ^json - [L,R=500]
</IfModule>
